(executable
    (public_name toplevel)
    (name toplevel)
    (libraries lambda parsing proof)
    (modules toplevel))

(executable
    (public_name pieuvre)
    (name pieuvre)
    (libraries lambda parsing)
    (modules pieuvre))
